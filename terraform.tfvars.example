# =============================================================================
# Terraform Variables 예제 파일
# =============================================================================
# 이 파일을 복사해서 terraform.tfvars로 이름을 변경하고 본인의 값으로 수정하세요.
# 
# 사용법:
# 1. cp terraform.tfvars.example terraform.tfvars
# 2. terraform.tfvars 파일에서 아래 값들을 본인의 환경에 맞게 수정
# 3. terraform init && terraform plan && terraform apply
#
# 주의사항:
# - terraform.tfvars 파일은 민감한 정보를 포함할 수 있으므로 Git에 커밋하지 마세요!
# - AWS SSO 또는 IAM 사용자 인증이 사전에 설정되어 있어야 합니다.
# =============================================================================

# -----------------------------------------------------------------------------
# AWS 인증 설정
# -----------------------------------------------------------------------------

# AWS Profile 이름 (필수)
# AWS CLI에서 설정한 프로필 이름을 입력하세요.
# 예: "default", "my-company-profile", "dev-profile" 등
aws_profile = "your-aws-profile-name"

# AWS 리전 (선택사항 - 기본값: ap-northeast-2)
# 한국 리전을 사용하는 경우 아래를 수정하세요.
aws_region = "ap-northeast-2"

# 다른 리전 사용 예시:
# aws_region = "us-east-1"      # 미국 동부 (버지니아)
# aws_region = "us-west-2"      # 미국 서부 (오레곤)
# aws_region = "ap-southeast-1" # 아시아 태평양 (싱가포르)

# -----------------------------------------------------------------------------
# 도메인 및 S3 설정 (필수)
# -----------------------------------------------------------------------------

# 메인 호스트 도메인 (필수)
# Route53에 이미 등록되어 있는 도메인을 입력하세요.
# 예: "example.com", "mycompany.com" 등
host_domain = "your-domain.com"

# CDN 서브도메인 (필수)
# CloudFront CDN에 사용할 서브도메인을 입력하세요.
# 일반적으로 메인 도메인의 서브도메인을 사용합니다.
# 예: "cdn.example.com", "assets.mycompany.com" 등
cdn_domain = "cdn.your-domain.com"

# S3 버킷 이름 (필수)
# 전 세계적으로 고유한 S3 버킷 이름을 입력하세요.
# 소문자, 숫자, 하이픈(-)만 사용 가능하며, 점(.)은 피하는 것을 권장합니다.
# 예: "my-company-cdn-assets-2024", "yourname-website-assets" 등
s3_bucket_name = "your-unique-bucket-name-2024"

# -----------------------------------------------------------------------------
# 사용 전 준비사항
# -----------------------------------------------------------------------------

# 1. AWS CLI 설치 및 인증 설정
#    - AWS SSO 사용 시: aws sso login --profile your-profile-name
#    - IAM 사용자 사용 시: aws configure --profile your-profile-name

# 2. Route53에 도메인이 이미 설정되어 있어야 합니다
#    위에서 설정한 host_domain이 Route53 호스팅 영역에 등록되어 있어야 합니다.
#    도메인이 없다면 AWS Route53에서 도메인을 구매하거나 외부에서 구매한 도메인을 등록하세요.

# 3. 변수 설정 확인사항
#    - host_domain: Route53에 등록된 실제 도메인
#    - cdn_domain: 보통 "cdn." + host_domain 형태로 설정
#    - s3_bucket_name: 전 세계적으로 고유한 이름이어야 함

# -----------------------------------------------------------------------------
# 배포 후 확인사항
# -----------------------------------------------------------------------------

# 1. S3 버킷에 파일 업로드 테스트
# 2. CloudFront 도메인으로 파일 접근 테스트
# 3. 커스텀 도메인(CDN 서브도메인)으로 접근 테스트
# 4. HTTPS 인증서 동작 확인

# -----------------------------------------------------------------------------
# 예상 비용 (참고용)
# -----------------------------------------------------------------------------

# - CloudFront: 데이터 전송량에 따라 과금 (첫 1TB 무료)
# - S3: 저장 용량 및 요청 수에 따라 과금 (첫 5GB 무료)
# - Route53: 호스팅 영역당 월 $0.50 + DNS 쿼리 비용
# - ACM: SSL/TLS 인증서 무료

# 상세한 요금 정보는 AWS 공식 문서를 참고하세요:
# https://aws.amazon.com/pricing/

# -----------------------------------------------------------------------------
# 설정 예시 (참고용)
# -----------------------------------------------------------------------------

# 예시 1: vibelist.click (실제 사용 예시)
# aws_profile = "boot-polcaneli"
# aws_region = "ap-northeast-2"
# host_domain = "vibelist.click"
# cdn_domain = "cdn.vibelist.click"
# s3_bucket_name = "vibelist-cdn-assets"

# 예시 2: 개인 프로젝트
# aws_profile = "default"
# host_domain = "myproject.com"
# cdn_domain = "cdn.myproject.com"
# s3_bucket_name = "myproject-cdn-assets-2024"

# 예시 3: 회사 프로젝트
# aws_profile = "company-aws"
# host_domain = "company.com"
# cdn_domain = "assets.company.com"
# s3_bucket_name = "company-production-cdn-2024"

# 예시 4: 개발 환경
# aws_profile = "dev-profile"
# host_domain = "dev.myapp.com"
# cdn_domain = "cdn-dev.myapp.com"
# s3_bucket_name = "myapp-dev-cdn-assets" 